# 项目当前真实状态审计

- 审计任务：S01 任务看板状态修复与审计
- 审计时间：2026-06-06 19:39:32 +08:00
- 审计范围：`docs/TASK_BOARD.md`、`agent.md`、`docs/status/Q40_*.md` 到 `docs/status/Q54_*.md`
- 安全边界：未读取、复制或输出 Key、Cookie、`.env`、管理密码、认证头、私钥或完整敏感连接串。

## 结论

当前主分支已经包含 Q40-Q53 的大量升级代码、文档和状态记录，但生产环境 `https://opc.ankangyu.cn` 尚未部署这些升级路由。Q41-Q51 和 Q53 应按本地完成记为 `done_local`，Q52 应记为 `blocked_production_validation`，Q54 应记为 `blocked_deploy`。

后续 Codex 不应继续开发新功能。下一步必须先恢复部署能力，把当前 `origin/main` 部署到腾讯云生产环境，再重跑 Q52 端到端验收和 Q54 生产升级验收。

## 已完成代码能力

- Q41：补齐产品升级数据库模型，覆盖多图商品、企业拍照、目标国家、全局聊天、报告版本和报告修改 proposal。
- Q42-Q43：完成多图商品录入后端和前端，支持 1-8 张商品图片、图片角色、主图、排序、草稿证据和确认链路。
- Q44-Q45：完成目标市场目录后端和动态国家选择前端，包含 19 国目录、区域/预设组合、CSV fallback、分析请求校验和看板展示。
- Q46：完成前端整体信息架构与视觉优化，覆盖导航、状态组件、首页、看板、报告、聊天入口和响应式检查。
- Q47-Q48：完成企业拍照录入后端和前端，支持企业图片上传、`company_drafts`、隐私过滤、草稿编辑、拒绝和确认入库。
- Q49-Q50：完成全局聊天后端和前端，使用会话式 `/api/chat`、受控上下文、脱敏裁剪、悬浮聊天窗口和安全错误态。
- Q51：完成聊天报告修改 proposal 和报告版本管理，采用只提议、不直接覆盖的确认链路，确认后追加 `report_versions`。
- Q53：完成比赛演示、部署和评委问答相关文档更新，并补充生产 compose/部署脚本配置。

## 未上线能力

生产环境仍缺少 Q40-Q53 新能力的完整部署。Q52/Q54 状态文件显示，生产可访问旧的健康检查、公司/产品、分析、看板和报告基础路由，但新增升级路由尚未上线。

因此，下列能力虽然已在主分支或本地验证中完成，但尚不能视为生产可用：

- 多图商品上传和多图草稿创建。
- 企业拍照上传和企业草稿创建。
- 目标市场国家目录和预设组合。
- 动态国家目录驱动的生产分析流程。
- 全局会话式聊天。
- 聊天生成报告修改 proposal。
- 报告版本列表、proposal 确认/拒绝和 append-only 恢复。
- `/companies/import` 企业拍照录入页面。

## 生产 404 路由

以下生产 404 来自 Q52/Q54 状态文件记录，代表生产环境尚未部署对应升级能力：

| 路由或页面 | 生产结果 | 影响 |
| --- | --- | --- |
| `POST /api/product-intake/screenshots` | 404 | 多图商品上传无法验收。 |
| `POST /api/company-intake/photo` | 404 | 企业拍照上传无法验收。 |
| `GET /api/company-intake/drafts` | 404 | 企业草稿列表/确认链路无法验收。 |
| `GET /api/markets/countries` | 404 | 目标国家目录无法加载。 |
| `GET /api/markets/presets` | 404 | 目标国家预设组合无法加载。 |
| `GET /api/chat/sessions?limit=1` | 404 | 全局会话式聊天无法验收。 |
| `GET /api/reports/1/versions` | 404 | 报告版本列表无法验收。 |
| `POST /api/reports/proposals/1/confirm` | 404 | 报告 proposal 确认无法验收。 |
| `GET /companies/import` | 404 | 企业拍照录入页面未上线。 |

## 下一步必须先做部署和验收

1. 恢复生产部署能力：修复 SSH 访问，或配置安全的部署流水线，或在腾讯云/宝塔终端直接执行部署。
2. 将当前 `origin/main` 部署到生产环境，并确保数据库迁移、上传目录和生产 compose 配置同步生效。
3. 部署后先做 HTTP preflight，确认上表 404 路由不再缺失。
4. 重跑 Q52 生产端到端验收，记录安全 demo 公司、产品、分析、报告、proposal 和版本 ID。
5. 重跑 Q54 生产升级验收，补充浏览器验证、回滚说明和最终交付状态。
6. 在 Q52/Q54 解除阻塞前，不新增功能任务，避免继续扩大主分支与生产环境之间的状态差距。
